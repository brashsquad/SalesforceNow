<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/card_background">
    <ImageView
        android:id="@+id/opportunity_logo"
        android:layout_height="35dp"
        android:layout_width="35dp"
        android:src="@drawable/stageleft_opportunity"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true" />
    <TextView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="@string/opportunities"
        android:layout_toRightOf="@+id/opportunity_logo"
        android:textAppearance="@android:style/TextAppearance.DeviceDefault.Large"
        android:layout_marginTop="5dp"
        android:layout_marginLeft="5dp"/>

    <ListView
        android:id="@+id/opportunity_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/card_background"
        android:layout_below="@+id/opportunity_logo">
    </ListView>
</RelativeLayout>